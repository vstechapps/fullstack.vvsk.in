{
  "name": "fullstack-vvsk-in-site-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx http-server docs -p 4321",
    "test": "playwright test",
    "test:headless": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "http-server": "^14.1.1"
  }
}
